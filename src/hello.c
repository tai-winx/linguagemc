/*Primeiro arquivo em linguagem em c.
Para iniciar iremos importar a biblioteca stdio (std-standard(padrão) io-input ouput (entrada e saída)).
Esta biblioteca possui os comandos para interação com o teclado e monitor*/
#include <stdio.h>

/*Iniciar a estrutura do código para a mensagem hello word, usando a função main(principal),
pois o compilador C irá procurar esta função para dar execução no nosso código.*/

int main(){
    printf ("Hello Word!\n");
    return 0;


}
